const cadenaNumeros = '4 2 3 4 5 1 2'
// Guardar en un vector solo los numeros
const vectorNumeros = cadenaNumeros.split(' ');
// Guardar en un vector solo los numeros
const vectorNumerosEnteros = vectorNumeros.map(Number);





if (entradaCadena === 'Error' || entradaCadena === '' ||
!Number.isNaN(valorArgumento) || entradaCadena === undefined) {
  
}